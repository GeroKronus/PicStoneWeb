# ========================================
# PicStone - Variáveis de Ambiente
# ========================================

# Banco de Dados
# Prioridade de detecção:
# 1. DATABASE_URL (PostgreSQL no Railway - definido automaticamente)
# 2. USE_SQLITE=true (SQLite para desenvolvimento local)
# 3. SQL_CONNECTION_STRING (SQL Server)

# Opção 1: PostgreSQL no Railway (RECOMENDADO)
# Railway define automaticamente quando você adiciona PostgreSQL
# DATABASE_URL=postgres://user:pass@host:port/database

# Opção 2: SQLite (Desenvolvimento Local)
USE_SQLITE=true

# Opção 3: SQL Server
# SQL_CONNECTION_STRING=Data Source=SEU_SERVIDOR,PORTA;Initial Catalog=SEU_BANCO;User ID=SEU_USUARIO;Password=SUA_SENHA;TrustServerCertificate=True;Encrypt=False;

# Autenticação JWT
# IMPORTANTE: Gere uma chave segura para produção
# Exemplo: openssl rand -base64 32
JWT_SECRET=SuaChaveSecretaSuperSegura123!@#$%

# Upload de Arquivos
UPLOAD_PATH=/app/uploads

# FTP (Opcional - para transferência de arquivos)
FTP_SERVER=ftp.seuservidor.com
FTP_USER=usuario_ftp
FTP_PASSWORD=senha_ftp

# Porta (Railway define automaticamente)
# PORT=8080
