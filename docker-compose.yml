version: '3.8'

services:
  # PostgreSQL - Idêntico ao Railway
  postgres:
    image: postgres:15-alpine
    container_name: picstone-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: railway
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: picstone123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - picstone-network

  # Backend ASP.NET Core
  backend:
    build:
      context: .
      dockerfile: ./Backend/Dockerfile
    container_name: picstone-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5000
      - DATABASE_URL=postgres://postgres:picstone123@postgres:5432/railway
      - JWT_SECRET=ChaveSecretaPadraoParaDesenvolvimento123!@#
      - NEXTAUTH_URL=http://localhost:5000
      - SMTP_HOST=smtppro.zoho.com
      - SMTP_PORT=465
      - SMTP_USER=contato@picstone.com.br
      - SMTP_PASSWORD=R0p4c44rl4$
      - EMAIL_FROM=contato@picstone.com.br
      - ADMIN_EMAIL=rogerio@picstone.com.br
    ports:
      - "5000:5000"
    volumes:
      - ./Backend/wwwroot:/app/wwwroot
      - ./Backend/uploads:/app/uploads
      - ./Backend/debug:/app/debug
      - ./WebP:/app/WebP
    networks:
      - picstone-network

volumes:
  postgres_data:

networks:
  picstone-network:
    driver: bridge
